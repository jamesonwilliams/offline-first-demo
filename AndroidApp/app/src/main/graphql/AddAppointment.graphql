mutation AddAppointment(
    $patientName: String!,
    $startTime: String!,
    $endTime: String!,
) {
    addAppointment(
        patientName: $patientName,
        startTime: $startTime,
        endTime: $endTime
    ) {
        ...appointmentFragment
    }
}
